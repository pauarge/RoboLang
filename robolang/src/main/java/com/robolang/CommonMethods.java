package com.robolang;

public enum CommonMethods {
    print, move_front, move_back, rotate_left, rotate_right, explore, move, waitToBePressed, followLine, shoot,
    forward, backward, waitForPress, setSpeed, setAcceleration, touched, beepSequence, buzz, beep, beepSequenceUp, party,
    getUltrasonicDistance, getValue
}